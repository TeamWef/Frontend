import { useNavigate } from "react-router-dom";

const ScheduleCard = () => {
  const navigate = useNavigate();
  return (
    <div>
      <hr />
      Schedule Card
    </div>
  );
};

export default ScheduleCard;
